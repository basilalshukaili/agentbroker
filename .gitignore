# Secrets — NEVER commit
.env
.env.local
.env.production
.env.*.local
*.pem
*.key
secrets/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST.in
# (deliberately NOT excluding the lowercase `manifest/` directory —
#  manifest/manifest.json is the source of truth for /manifest, /openapi.yaml,
#  /llms.txt, /.well-known/*, MCP tools/list. Without it the service 500s.)

# Virtualenv
venv/
.venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test artifacts
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.cache
*.log

# Reports (generated)
reports/*.json
reports/*.md
!reports/.gitkeep

# Local dev
*.db
*.sqlite
*.sqlite3

# Playwright
playwright-report/
test-results/
.cache/ms-playwright/

# Internal company vault — CEO tick logs, board state, spend figures.
# This repo is PUBLIC. A tick writes here and a stray `git add -A` published
# one of these on 2026-09-01 (ops notes only, no secrets). Never again.
obsidian-vault/
