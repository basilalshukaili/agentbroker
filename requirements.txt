# SMB Transaction & Communication Broker — Python dependencies
# Python 3.11+

# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# --- Data validation ---
pydantic==2.7.1
pydantic-settings==2.2.1

# --- Async task queue ---
celery==5.3.6
redis==5.0.4

# --- Database ---
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# --- Channel adapters ---
twilio==9.0.4
sendgrid==6.11.0
httpx==0.27.0             # Vapi / Bland / Cal.com REST calls
playwright==1.44.0        # Browser automation (last-resort channel)

# --- Compliance ---
phonenumbers==8.13.37     # Phone number validation / jurisdiction detection

# --- Telemetry ---
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp==1.24.0

# --- Utilities ---
python-dateutil==2.9.0
python-jose[cryptography]==3.3.0   # JWT (production replacement for stub)
passlib[bcrypt]==1.7.4
tenacity==8.2.3           # Retry logic (supplements custom retry_policy)

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
anyio==4.3.0

# --- Dev tools ---
black==24.4.2
ruff==0.4.4
mypy==1.10.0
