# GENERATED by scripts/gen_manifests.py from registry/servers.yaml.
# Do not edit by hand: CI compares this file against the generator and fails
# on any difference. Change servers.yaml instead.
startCommand:
  type: http
  url: "https://api.hatchloop.dev/mcp"
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: API Key (optional)
        description: >
          Get one at https://hatchloop.dev/agent-broker/
          Pass as request header: X-Agent-Identity: <your-key>.
          15 of the 23 tools require no auth (12 always-free + 3 free within a daily quota). Write
          ops require an X-Agent-Identity bearer token: a free email-verified key gives 100
          ops/day, or buy credits at https://hatchloop.dev/pricing
    required: []
  exampleConfig:
    apiKey: ""
