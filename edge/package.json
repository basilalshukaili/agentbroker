{
  "name": "agent-broker-edge",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker edge front-door for Agent Broker MCP service. Caches discovery endpoints at edge, proxies tool execution to Render origin with retry-on-cold-start.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251201.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "hono": "^4.6.0"
  }
}
